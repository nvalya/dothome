[user]
    name = Valentin Novikov
    email = vn-git@yandex.ru
    signingkey = 4661797E
[core]
    editor = vim
    quotepath = false
    excludesfile = ~/.gitignore_global
    autocrlf = input
[alias]
    commit = commit -S
    c = commit -S
    cs = commit -S
    cl = clone --recursive
    clm = clone --branch master
    logs = log --show-signature
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    st = status
    co = checkout
    br = branch
    type = cat-file -t
    dump = cat-file -p
    branches = branch -a
    tags = tag -a
